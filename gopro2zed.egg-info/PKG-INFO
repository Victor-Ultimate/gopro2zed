Metadata-Version: 2.4
Name: gopro2zed
Version: 0.1.0
Summary: 将 GoPro 鱼眼图像转换为 ZED Mini 风格的针孔图像
Author: gopro2zed
License: MIT
Project-URL: Repository, https://github.com/your-repo/gopro2zed
Keywords: fisheye,zed,opencv,camera,calibration
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Topic :: Scientific/Engineering :: Image Processing
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: numpy>=1.20
Requires-Dist: opencv-python>=4.5
Requires-Dist: PyYAML>=5.0
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"

# gopro2zed

将 GoPro 鱼眼图像转换为 ZED Mini 风格的针孔（rectified）图像。

## 安装

```bash
# 从源码安装（在 gopro2zed 目录下）
cd gopro2zed
pip install .
```

或从父目录安装：

```bash
pip install ./gopro2zed
```

## 用法

### 命令行

```bash
gopro2zed --image input.jpg --source-json calibration.json --output output.png
```

可选参数：

- `--target-zed-yaml`：目标 ZED 标定 YAML，不指定则使用内置 ZED Mini 默认参数
- `--target-camera-key`：YAML 中的相机 key，默认 `left`
- `--aspect-mode`：`fy_over_fx` 或 `fx_over_fy`，默认 `fy_over_fx`
- `--cuda`：启用 CUDA GPU 加速（需安装带 CUDA 的 OpenCV 构建版本）

示例（使用自定义 ZED 标定）：

```bash
gopro2zed --image test.jpg --source-json gopro_calib.json \
  --target-zed-yaml zed_calibration.yaml --target-camera-key left \
  --output out.png
```

### Python API

```python
from gopro2zed import (
    load_source_fisheye_json,
    load_target_zed_yaml,
    default_zed_mini_target,
    fisheye_to_target_pinhole,
)

# 加载标定
source = load_source_fisheye_json("gopro_calib.json")
target = load_target_zed_yaml("zed_calibration.yaml", camera_key="left")
# 或使用默认: target = default_zed_mini_target()

# 转换（可选 use_cuda=True 启用 GPU 加速）
fisheye_to_target_pinhole(
    image_path="input.jpg",
    output_path="output.png",
    source_K=source["K"],
    source_D=source["D"],
    target_K=target["K"],
    target_size=target["resolution"],
    use_cuda=True,  # 需 OpenCV 带 CUDA 构建
)
```

## 依赖

- numpy
- opencv-python
- PyYAML

**CUDA 加速**：`--cuda` 需 OpenCV 带 CUDA 编译。标准 PyPI 的 `opencv-python` 不含 CUDA，需使用自定义构建或支持 CUDA 的发行版。

**CUDA 加速**：`--cuda` 选项需要 OpenCV 编译时启用 CUDA 支持。标准 PyPI 的 `opencv-python` 不含 CUDA，需自行编译或使用带 CUDA 的预编译版本（如 `opencv-contrib-python` 的 CUDA 构建）。

**CUDA 加速**：`--cuda` 需要 OpenCV 带 CUDA 的构建版本。标准 `opencv-python` 不含 CUDA，需自行编译或使用提供 CUDA 的发行版。

**CUDA 加速**：`--cuda` 选项需要 OpenCV 编译了 CUDA 支持。标准 `opencv-python` 不含 CUDA，需使用带 CUDA 的自定义构建或 `opencv-contrib-python`（含 CUDA 版本）。

**CUDA 加速**：`--cuda` 需要 OpenCV 编译时启用 CUDA。标准 `opencv-python` 不含 CUDA，需使用自定义编译版本或带 CUDA 的 `opencv-contrib-python`。CUDA 不可用时将自动回退到 CPU。
